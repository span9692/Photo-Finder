import { csrfFetch } from './csrf';

//action creator

//thunk functions

//reducer
const reviewReducer = (state = {}, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default reviewReducer;
